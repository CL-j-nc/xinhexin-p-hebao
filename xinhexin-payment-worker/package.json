{
    "name": "xinhexin-payment-worker",
    "version": "1.0.0",
    "description": "Payment link generation worker using Puppeteer and AI",
    "main": "src/index.ts",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "deploy": "wrangler deploy"
    },
    "devDependencies": {
        "@cloudflare/puppeteer": "^1.0.6",
        "@cloudflare/workers-types": "^4.20240925.0",
        "typescript": "^5.6.2",
        "wrangler": "^4.64.0"
    }
}
